# <Area> Function Catalog

## Scope

Describe which runtime/tool/service area this catalog covers.

## Functions

### <functionName>()

Status: active / planned / deprecated / removed

Owner/runtime:
- Mac / Shelly / Home Assistant / shared

Source:
- `<path>`

Purpose:
- What this function is responsible for.

Inputs:
- Input name/type/shape.

Outputs:
- Return value or produced data.

Side effects:
- KVS, RPC, HTTP, file write, component update, script start/stop, logs, actuators, none.

Contract notes:
- Constraints, invariants, schema assumptions, fallback behavior.

Used by:
- Runtime roles, services, callers or packages.

Tests:
- Test files or package test cases.

Introduced:
- P____

Last changed:
- P____

Notes:
- Durable notes only. Put package-local thinking in `requirements/package-runs/<Pxxxx>/functions.md`.
