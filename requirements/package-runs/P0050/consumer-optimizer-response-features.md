# P0050 consumer optimizer response features

Backward-looking top/bottom exposure counters were computed for 6h, 12h and 24h windows. Next recovery fields are explicitly `_oracle` explanatory diagnostics.
