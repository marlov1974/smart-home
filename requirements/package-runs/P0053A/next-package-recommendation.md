# P0053A next package recommendation

Use `physical_balance_flow_exchange_analysis_v1` for offline feature attribution before deciding whether observed A09/A11 signals should become training features. They are historical observed signals and are not forecast-safe without a separate future-availability package.
