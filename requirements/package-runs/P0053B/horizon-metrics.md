# P0053B horizon metrics

Best forecast-safe model: `M4_Ridge_G4_calendar_load_lags_weather` at horizon `1` with holdout MAE `6.935`.

Best baseline: `pred_B0_same_hour_previous_day` at horizon `1` with holdout MAE `13.390`.

Full metric tables are in `horizon-metrics.csv` and `metrics-summary.json`.
