# P0053C-B Forecast-Origin Log Output

Local table:

```text
m4_48h_anchored_absolute_price_forecast_log_p0053cb_v1
```

Rows persisted:

```text
82656
```

The log contains `prediction_kind=anchored_absolute_price`, `area=SE1`, and `prediction_unit=source_hour_price_unit`. It stores the selected validation-only anchor for validation and holdout forecast origins.

Sample:

```text
requirements/package-runs/P0053C-B/forecast-origin-log-sample.csv
```
