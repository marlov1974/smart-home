# P0054D LABB

Additional dependency-safe advanced model: ExtraTreesRegressor on the same G4 `se4_load_weather` row set as HGB and MLP. Deep/sequence and XGBoost/LightGBM dependencies are not locally available.
