# P0054L2 Model Checkpoints

This directory stores immediate per-model evidence for the serial P0054L2 LABB run.

Each completed, skipped or failed model writes:

```text
<model>.md
<model>-metrics.json
```

These files are accepted independently so a later slow model does not invalidate earlier completed evidence.
