# P0056D Next Package Recommendation

Retry P0056D after Open-Meteo archive rate limit cooldown. If 429 persists, create a follow-up package for slower scheduled FI fetching with one representative location per operator-approved run and no model retest until all FI locations are complete.

