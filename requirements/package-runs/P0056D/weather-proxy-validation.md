# P0056D Weather Proxy Validation

`STOP`

SE1 and SE2 representative location weather rows are complete. FI is incomplete due Open-Meteo `HTTP 429 Too Many Requests`, so weighted area proxy/features were not built and forecast retest was not started.

