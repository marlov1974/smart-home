# P0056D Zone Weighting Method

Weights are deterministic manual first-pass load-centre approximations. No local population or load-share metadata was found during bootstrap.

The weights sum to 1.0 per area. Confidence is `medium` for major load-centre zones and `low` for smaller or broader climate-representation zones.

