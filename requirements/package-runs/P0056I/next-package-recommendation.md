# P0056I Next Package Recommendation

Use `TWX` as the train-window assumption in the next SE2 emulator consumption protocol test, then test whether the remaining static-baseline gap is caused by model method or evaluation split rather than training-window length.
